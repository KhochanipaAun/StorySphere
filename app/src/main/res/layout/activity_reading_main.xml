<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ReadingMainActivity">

    <!-- AppBar / Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:fitsSystemWindows="true">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/white"
            app:navigationIcon="@drawable/round_arrow_back_ios_24"
            app:title=""
            app:layout_scrollFlags="scroll|enterAlways"
            app:menu="@menu/menu_reading_main" />
        <!-- menu_reading_main มี item action_report ไอคอน more_vert -->
    </com.google.android.material.appbar.AppBarLayout>

    <!-- เนื้อหาเลื่อน -->
    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:background="@color/white"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior"
        android:paddingBottom="72dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="12dp">

            <!-- การ์ดเรื่อง (รูป/ชื่อ/คำโปรย/สถิติ) -->
            <include
                android:id="@+id/storyCard"
                layout="@layout/item_story_reading"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ไอคอน Tag -->
            <ImageView
                android:id="@+id/imgHome"
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:layout_marginTop="16dp"
                android:layout_marginLeft="10dp"
                app:srcCompat="@drawable/ic_tag"
                app:layout_constraintTop_toBottomOf="@id/storyCard"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- พื้นที่วางแท็ก (จะเติมจากหลังบ้าน) -->
            <LinearLayout
                android:id="@+id/tagContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="start"
                android:layout_marginStart="8dp"
                android:layout_marginTop="10dp"
                app:layout_constraintTop_toBottomOf="@id/storyCard"
                app:layout_constraintStart_toEndOf="@id/imgHome"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- ตัวอย่างชิปสำหรับพรีวิว (ลบได้ภายหลัง) -->
                <include layout="@layout/item_tag"/>
                <Space android:layout_width="8dp" android:layout_height="wrap_content"/>
                <include layout="@layout/item_tag"/>
                <Space android:layout_width="8dp" android:layout_height="wrap_content"/>
                <include layout="@layout/item_tag"/>
            </LinearLayout>

            <!-- หัวข้อ All episodes -->

            <!-- เส้นคั่น -->

            <!-- รายการตอนอ่านได้ -->

            <TextView
                android:id="@+id/textAllEpisodes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_marginTop="40dp"
                android:text="All episodes"
                android:textColor="@color/purple1"
                android:textSize="18sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tagContainer" />

            <View
                android:id="@+id/divider"
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_marginTop="4dp"
                android:background="@color/light_gray"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.0"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/textAllEpisodes" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewEpisodes"
                android:layout_width="0dp"
                android:layout_height="580dp"
                android:layout_marginTop="8dp"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"
                android:paddingBottom="8dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.0"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/divider"
                app:layout_constraintVertical_bias="0.894"
                tools:listitem="@layout/item_episode_reading" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>

    <!-- แถบล่าง -->
    <!-- Bottom Nav -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="95dp"
        android:layout_gravity="bottom"
        android:background="@color/white"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/purple1"
        app:itemTextColor="@color/purple1"
        app:labelVisibilityMode="labeled"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
