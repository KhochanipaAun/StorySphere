<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".Writing_Add_Episode3">

    <!-- AppBar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:toolbarId="@+id/toolbar"
            app:layout_scrollFlags="scroll|enterAlways|enterAlwaysCollapsed"
            app:contentScrim="@color/white"
            android:background="@color/white"
            android:fitsSystemWindows="true">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/white"
                app:layout_collapseMode="pin"
                app:navigationIcon="@drawable/round_arrow_back_ios_24" />

            <TextView
                android:id="@+id/textView4"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="330dp"
                android:layout_marginTop="20dp"
                android:letterSpacing="0.02"
                android:text="Delete"
                android:textColor="@color/greyText"
                android:textSize="20sp" />

        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- เนื้อหาเลื่อน -->
    <ScrollView
        android:id="@+id/nestedScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:fillViewport="true"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/main"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:background="@color/white">

            <!-- การ์ดข้อมูลเรื่อง (รวมรูป/ชื่อ/คำโปรย/สถิติ) -->
            <include
                android:id="@+id/storyCard"
                layout="@layout/item_story"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- ไอคอนแท็ก -->
            <ImageView
                android:id="@+id/imgHome"
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:layout_marginTop="16dp"
                android:layout_marginLeft="10dp"
                app:srcCompat="@drawable/ic_tag"
                app:layout_constraintTop_toBottomOf="@id/storyCard"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- พื้นที่วางแท็ก (จะเติมจากหลังบ้าน) -->
            <LinearLayout
                android:id="@+id/tagContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="start"
                android:layout_marginStart="8dp"
                android:layout_marginTop="10dp"
                app:layout_constraintTop_toBottomOf="@id/storyCard"
                app:layout_constraintStart_toEndOf="@id/imgHome"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- ใส่ชิพตัวอย่างเพื่อดูใน Design (ลบ/เพิ่มได้) -->
                <include layout="@layout/item_tag"/>
                <Space android:layout_width="8dp" android:layout_height="wrap_content"/>
                <include layout="@layout/item_tag"/>
                <Space android:layout_width="8dp" android:layout_height="wrap_content"/>
                <include layout="@layout/item_tag"/>

            </LinearLayout>

            <!-- ปุ่มเพิ่มตอน -->
            <Button
                android:id="@+id/btnAddEpisode"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Add new episode"
                android:textColor="@color/white"
                android:paddingHorizontal="24dp"
                android:paddingVertical="10dp"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toBottomOf="@id/tagContainer"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- หัวข้อ All episodes -->
            <TextView
                android:id="@+id/textAllEpisodes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:layout_marginLeft="10dp"
                android:text="All episodes"
                android:textColor="@color/purple1"
                android:textSize="18sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/btnAddEpisode" />

            <!-- เส้นคั่น -->
            <View
                android:id="@+id/dividerAllEpisodes"
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_marginTop="8dp"
                android:background="@color/light_gray"
                app:layout_constraintTop_toBottomOf="@id/textAllEpisodes"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- รายการตอน (ใช้ item_episode เป็น list item) -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewEpisodes"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"
                android:paddingBottom="16dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/dividerAllEpisodes"
                tools:listitem="@layout/item_episode" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>

